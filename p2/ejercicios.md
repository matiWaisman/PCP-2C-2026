# Ejercicio 1 

```
global semaphore puedeF = Semaphore(0)
global semaphore puedeC = Semaphore(0)

thread t1 {
    print(A)
    puedeF.
    print(B)
    print(C)
}

thread t2 {
    print(E)
    print(F)
    print(G)
}
```